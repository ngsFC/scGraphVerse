% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JRF_internal.R
\name{JRF_internal}
\alias{JRF_internal}
\title{Internal JRF Network Inference Function (Simplified)}
\usage{
JRF_internal(X, ntree = 500, mtry = NULL, genes.name = NULL)
}
\arguments{
\item{X}{A list of expression matrices}

\item{ntree}{Number of trees in the random forest. Default: 500.}

\item{mtry}{Number of variables to sample at each tree node. If NULL, defaults to sqrt(p).}

\item{genes.name}{Character vector of gene names. If NULL, generates names.}
}
\value{
A data frame with gene pairs and importance scores
}
\description{
This function provides JRF network inference by delegating to the
original working JRF package instead of reimplementing the algorithm.
}
\examples{
# Create sample expression data as a list of matrices
n_genes <- 5
n_samples <- 20

# Generate two expression matrices
X <- list(
  matrix(rnorm(n_genes * n_samples, mean = 5, sd = 2), 
         nrow = n_genes, ncol = n_samples),
  matrix(rnorm(n_genes * n_samples, mean = 4, sd = 1.5), 
         nrow = n_genes, ncol = n_samples)
)

# Add gene names
rownames(X[[1]]) <- rownames(X[[2]]) <- paste0("Gene", 1:n_genes)

# Run JRF network inference (requires JRF package)
if (requireNamespace("JRF", quietly = TRUE)) {
  result <- JRF_internal(X, ntree = 50, genes.name = paste0("Gene", 1:n_genes))
}
}
